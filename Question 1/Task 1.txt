writefile = open("Combined_text_file.txt", 'w')
readtext = open("CSV1.csv",'r')
sum1=0
for line in readtext:
    wordlist = line.split()
    for word in wordlist:
        writefile.write(word + " ")

readtext = open("CSV2.csv",'r')
sum2=0
for line in readtext:
    wordlist = line.split()
    for word in wordlist:
        writefile.write(word + " ")
        
readtext = open("CSV3.csv",'r')
sum3=0
for line in readtext:
    wordlist = line.split()
    for word in wordlist:
        writefile.write(word + " ")
        
readtext = open("CSV4.csv",'r')
sum4=0
for line in readtext:
    wordlist = line.split()
    for word in wordlist:
        writefile.write(word + " ")
writefile.close        
print ("Complete")

